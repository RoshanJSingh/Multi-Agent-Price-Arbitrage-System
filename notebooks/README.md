# Research Notebooks

These notebooks preserve the course-project experimentation trail:

- `deal_agent_workflow/`: agent workflow, deal scanning, service wiring, and dashboard experiments.
- `frontier_finetuning/`: Amazon product curation, baseline models, frontier model experiments, and fine-tuning runs.
- `open_source_finetuning/`: open-source model fine-tuning experiments.
- `rag_pipeline/`: retrieval, ChromaDB, embeddings, and context experiments.

Notebook outputs are retained only where they support the development trail. Runtime secrets should always be supplied through environment variables.

