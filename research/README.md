# Research

This folder preserves legacy course-project source files that informed the cleaned `src/deal_finder` package. The production-facing implementation lives under `src/`; this folder is useful for tracing the original notebook-to-application progression.

